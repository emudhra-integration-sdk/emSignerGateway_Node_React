# Install below dependencies
`npm install cors`
`npm install express`
`npm install uuid`


# Run service
`node .\service.js`

# Get Parameters for emSigner gateway
endpoint: [`\emSignerParameters`]
method: `GET` or `POST` (Note: When using the POST method, send the emSigner Request JSON in the request.)
response: `appilication/json`

Do form post to the gateway URL with below params
`Parameter1`
`Parameter2`
`Parameter3`